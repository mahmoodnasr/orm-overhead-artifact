# The SF1 rerun's frozen stack — SF1_RERUN_PLAN.md G2, frozen 2026-08-31,
# verified against PyPI, Docker Hub, MCR and the mssql-django support matrix.
#
# Django 6.0.8 is a ceiling, not a preference: 6.1 exists, and mssql-django
# 1.8.0 supports Django up to 6.0. Python 3.12+ is forced by Django 6.0.
#
# ONE MySQL DRIVER, SHARED BY BOTH FRAMEWORKS (C31). mysqlclient, not the
# PyMySQL install_as_MySQLdb shim: Django 6.0 gates its MySQL backend at
# mysqlclient >= 2.2.1 and PyMySQL reports 1.4.6, so the shim raises at
# import. SQLAlchemy moves with it, to mysql+mysqldb:// — a C extension
# under one framework and a pure-Python driver under the other would be
# charged to the framework, which is the defect this file exists to avoid.
Django==6.0.8
SQLAlchemy==2.0.52
mssql-django==1.8.0

# Drivers: one per DBMS, both frameworks on the same one.
psycopg2-binary==2.9.12
mysqlclient==2.2.8
oracledb==4.0.2
pyodbc==5.3.0

# Data generation.
duckdb==1.5.5

# Analysis. pandas is held below 3.0 deliberately: the ten modules under
# scripts/3-moef and scripts/4-analysis were written against pandas 2.x and
# 3.0 is a breaking release. Lift it only after those modules are re-run.
numpy==2.5.2
scipy==1.16.3
pandas>=2.2,<3
matplotlib==3.10.7
seaborn==0.13.2
psutil==7.1.3
