# Benchmark runtime: Python 3.12, matching the recorded SF1 framework/driver stack.
# Analysis of the supplied measurements needs requirements-analysis.txt only.
# mysqlclient requires MySQL client development libraries and pkg-config.
# pyodbc requires an ODBC driver manager and the selected database's ODBC driver.
Django==6.0.8
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12
mysqlclient==2.2.8
oracledb==4.0.2
pyodbc==5.3.0
mssql-django==1.8.0
duckdb==1.5.5
psutil==7.1.3
